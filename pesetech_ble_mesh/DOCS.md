# Setup

1. Install and configure an MQTT broker in Home Assistant.
2. Copy the Pesetech mesh export to `/share/pesetech_mesh.json`.
3. Start the add-on.

The imported skylights appear automatically through MQTT discovery.
