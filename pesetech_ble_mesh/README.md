# Pesetech BLE Mesh Gateway

Controls imported Pesetech/Lepu artificial skylights through Home Assistant MQTT discovery.

Copy the Pesetech mesh export to `/share/pesetech_mesh.json`, then start the add-on. The first start imports the mesh; later starts reuse the saved configuration.
